<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>agraX — Commodity data for the produce supply chain</title>
<meta name="description" content="agraX turns USDA Market News into a live view of the produce supply chain — for buyers, growers, shippers, and analysts." />
<link rel="icon" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 36 36'%3E%3Crect width='36' height='36' rx='8' fill='%230A0B0A'/%3E%3Ccircle cx='18' cy='18' r='4' fill='%2315A24A'/%3E%3C/svg%3E" />
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=JetBrains+Mono:wght@400;500;600&display=swap" rel="stylesheet" />
<link rel="stylesheet" href="/styles/tokens.css" />
<link rel="stylesheet" href="/styles/components.css" />
<style>
body { background: var(--page-warm); overflow-x: hidden; }
.lp { max-width: 1200px; margin: 0 auto; padding: 0 24px; }

.ticker { background: var(--dark-1); color: #fff; overflow: hidden; border-bottom: 0.5px solid var(--dark-6); padding: 10px 0; font-family: var(--font-mono); font-size: 11.5px; }
.ticker__wrap { display: flex; gap: 40px; white-space: nowrap; animation: tk 60s linear infinite; }
.ticker__wrap:hover { animation-play-state: paused; }
@keyframes tk { from { transform: translateX(0); } to { transform: translateX(-50%); } }
.tk-item { display: inline-flex; align-items: center; gap: 8px; color: var(--dark-fg-muted); }
.tk-item b { color: #fff; font-weight: 600; }
.tk-item .up { color: var(--pop-glow); }
.tk-item .dn { color: #ff8080; }
.tk-live { display: inline-flex; align-items: center; gap: 6px; color: var(--pop-glow); font-size: 10.5px; font-weight: 600; letter-spacing: 0.1em; text-transform: uppercase; padding: 0 20px; }
.tk-live__dot { width: 6px; height: 6px; border-radius: 50%; background: var(--pop); box-shadow: 0 0 8px var(--pop); animation: pulse 1.6s ease-in-out infinite; }
@keyframes pulse { 0%,100% { opacity: 1; } 50% { opacity: 0.4; } }

.nav { position: sticky; top: 0; z-index: 50; background: rgba(250,250,247,0.86); backdrop-filter: blur(12px); border-bottom: 0.5px solid var(--line); }
.nav__wrap { display: flex; align-items: center; gap: 32px; padding: 14px 0; }
.nav__logo { display: flex; align-items: center; gap: 9px; }
.nav__mark { width: 26px; height: 26px; border-radius: 7px; background: var(--dark-1); display: flex; align-items: center; justify-content: center; }
.nav__mark span { width: 9px; height: 9px; border-radius: 50%; background: var(--pop); box-shadow: 0 0 8px var(--pop); }
.nav__word { font-size: 18px; font-weight: 600; letter-spacing: var(--ls-display-lg); }
.nav__links { display: none; gap: 26px; font-size: 13px; font-weight: 500; color: var(--sub); }
@media(min-width: 780px){ .nav__links { display: flex; } }
.nav__cta { margin-left: auto; display: flex; gap: 8px; align-items: center; }
.nav__cta a.sign { font-size: 13px; color: var(--sub); }

.hero { position: relative; padding: 48px 0 64px; text-align: center; }
.hero::before { content: ''; position: absolute; inset: 0; background: var(--glow-hero-top), var(--glow-hero-l), var(--glow-hero-r); pointer-events: none; z-index: 0; }
.hero > * { position: relative; z-index: 1; }
.hero__eyebrow { display: inline-flex; align-items: center; gap: 7px; background: var(--card); border: 0.5px solid var(--line); border-radius: 20px; padding: 5px 13px; font-size: 11.5px; font-weight: 500; color: var(--sub); }
.hero__eyebrow-dot { width: 6px; height: 6px; border-radius: 50%; background: var(--pop); box-shadow: 0 0 0 3px rgba(21,162,74,.15); animation: pulse 1.6s ease-in-out infinite; }
.hero__pill { display: inline-block; margin-top: 22px; }
.hero h1 { font-size: 42px; line-height: 1.02; font-weight: 600; letter-spacing: var(--ls-display-xl); margin: 22px auto 0; max-width: 820px; }
@media(min-width: 700px){ .hero h1 { font-size: 58px; } }
.hero h1 .hl { background: linear-gradient(180deg, transparent 65%, var(--pop-35) 65%); padding: 0 4px; }
.hero__sub { font-size: 16px; color: var(--sub); max-width: 560px; margin: 20px auto 0; line-height: 1.5; }
.hero__meta { display: inline-flex; align-items: center; gap: 20px; flex-wrap: wrap; margin-top: 28px; padding: 12px 20px; background: var(--card); border: 0.5px solid var(--line); border-radius: 14px; }
.hero__meta-item { display: flex; flex-direction: column; align-items: flex-start; gap: 1px; }
.hero__meta-lbl { font-size: 10px; color: var(--muted); letter-spacing: 0.1em; text-transform: uppercase; font-weight: 600; }
.hero__meta-val { font-family: var(--font-mono); font-size: 14px; font-weight: 600; color: var(--ink); }
.hero__meta-val.pos { color: var(--pop); }
.hero__gauge-wrap { display: flex; align-items: center; gap: 10px; }
.hero__gauge { position: relative; width: 40px; height: 40px; }
.hero__gauge svg { transform: rotate(-90deg); }
.hero__gauge-txt { position: absolute; inset: 0; display: flex; align-items: center; justify-content: center; font-family: var(--font-mono); font-size: 11px; font-weight: 600; color: var(--pop); }

.sect { padding: 72px 0; position: relative; }
.sect__head { text-align: center; margin-bottom: 40px; }
.sect__eyebrow { font-size: 11px; font-weight: 600; letter-spacing: 0.14em; text-transform: uppercase; color: var(--pop); }
.sect__title { font-size: 32px; font-weight: 600; letter-spacing: var(--ls-display-lg); margin-top: 10px; line-height: 1.1; }
@media(min-width: 700px){ .sect__title { font-size: 40px; } }
.sect__sub { font-size: 15px; color: var(--sub); max-width: 540px; margin: 12px auto 0; }

.checker { background: var(--card); border: 0.5px solid var(--line); border-radius: 16px; padding: 24px; max-width: 720px; margin: 0 auto; box-shadow: var(--shadow-md); }
.checker__row { display: grid; grid-template-columns: 1fr; gap: 10px; }
@media(min-width: 640px){ .checker__row { grid-template-columns: 1fr 1fr auto; align-items: end; } }
.checker__label { font-size: 10.5px; font-weight: 600; letter-spacing: 0.1em; text-transform: uppercase; color: var(--muted); margin-bottom: 6px; }
.checker__sel { width: 100%; background: var(--fill); border: 0.5px solid transparent; border-radius: 9px; padding: 10px 14px; font-family: var(--font-body); font-size: 14px; color: var(--ink); appearance: none; cursor: pointer; }
.checker__sel:focus { outline: none; border-color: var(--pop); background: var(--card); }
.checker__chips { display: flex; gap: 6px; flex-wrap: wrap; margin-top: 14px; }
.checker__chip { font-size: 11.5px; font-weight: 500; padding: 5px 11px; border-radius: 20px; background: var(--pop-08); color: var(--pop); cursor: pointer; }
.checker__chip:hover { background: var(--pop-14); }
.checker__result { margin-top: 20px; padding-top: 20px; border-top: 0.5px solid var(--line-soft); display: none; }
.checker__result.show { display: block; }
.checker__price { font-family: var(--font-mono); font-size: 32px; font-weight: 600; letter-spacing: var(--ls-num); line-height: 1; }
.checker__delta { font-family: var(--font-mono); font-size: 13px; font-weight: 600; padding: 3px 9px; border-radius: 6px; }
.checker__delta.dn { background: var(--pop-09); color: var(--pop); }
.checker__delta.up { background: var(--up-08); color: var(--up); }
.checker__delta.fl { background: var(--fill); color: var(--muted); }
.checker__meta { font-size: 12px; color: var(--muted); margin-top: 6px; }

.board { display: grid; grid-template-columns: 1fr; gap: 12px; }
@media(min-width: 640px){ .board { grid-template-columns: 1fr 1fr; } }
@media(min-width: 1000px){ .board { grid-template-columns: repeat(4, 1fr); } }
.board__col { background: var(--card); border: 0.5px solid var(--line); border-radius: 14px; overflow: hidden; }
.board__hd { padding: 14px 16px; border-bottom: 0.5px solid var(--line-soft); display: flex; align-items: center; gap: 8px; }
.board__pin { width: 8px; height: 8px; border-radius: 50%; background: var(--pop); box-shadow: 0 0 6px var(--pop); }
.board__city { font-size: 13.5px; font-weight: 600; }
.board__count { margin-left: auto; font-family: var(--font-mono); font-size: 11px; color: var(--muted); }
.board__row { display: flex; align-items: center; gap: 8px; padding: 11px 16px; border-top: 0.5px solid var(--line-softer); font-size: 12.5px; }
.board__row:first-of-type { border-top: none; }
.board__row-name { flex: 1; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.board__row-price { font-family: var(--font-mono); font-weight: 500; }
.board__row-chg { font-family: var(--font-mono); font-size: 10.5px; font-weight: 600; min-width: 44px; text-align: right; }

.map-card { background: var(--grad-card-mint); border: 0.5px solid #E4EBD6; border-radius: 20px; padding: 40px 24px; position: relative; overflow: hidden; text-align: center; }
.map-card::before { content: ''; position: absolute; top: -60px; right: -60px; width: 200px; height: 200px; border-radius: 50%; background: var(--glow-corner-soft); pointer-events: none; }
.map-card::after { content: ''; position: absolute; inset: 0; background-image: var(--dotgrid); background-size: var(--dotgrid-size); opacity: 0.5; pointer-events: none; }
.map-card > * { position: relative; z-index: 1; }
.map-svg { width: 100%; max-width: 720px; margin: 20px auto 0; }
.map-svg circle { fill: var(--pop); }
.map-pulse { animation: pulse 2s ease-in-out infinite; }
.map-caption { font-size: 12px; color: var(--sub); margin-top: 24px; font-family: var(--font-mono); }

.features { display: grid; grid-template-columns: 1fr; gap: 20px; }
@media(min-width: 900px){ .features { grid-template-columns: 1fr 1fr; } }
.feature { background: var(--card); border: 0.5px solid var(--line); border-radius: 16px; padding: 32px 28px; position: relative; overflow: hidden; }
.feature__ico { font-size: 22px; color: var(--pop); margin-bottom: 14px; }
.feature__title { font-size: 20px; font-weight: 600; letter-spacing: var(--ls-display); }
.feature__desc { font-size: 14px; color: var(--sub); margin-top: 8px; line-height: 1.55; }
.feature__mini { margin-top: 20px; background: var(--card-alt); border: 0.5px solid var(--line-soft); border-radius: 10px; padding: 14px; font-family: var(--font-mono); font-size: 11px; }
.feature__mini-row { display: flex; justify-content: space-between; padding: 4px 0; }
.feature__mini-row + .feature__mini-row { border-top: 0.5px solid var(--line-softer); }
.feature__mini-row b { color: var(--ink); font-weight: 600; }
.feature__mini-row .pos { color: var(--pop); }
.feature__mini-row .neg { color: var(--up); }

.audiences { display: grid; grid-template-columns: 1fr; gap: 16px; }
@media(min-width: 640px){ .audiences { grid-template-columns: 1fr 1fr; } }
@media(min-width: 1000px){ .audiences { grid-template-columns: repeat(4, 1fr); } }
.aud { background: var(--card); border: 0.5px solid var(--line); border-radius: 14px; padding: 24px; }
.aud__who { font-size: 11px; font-weight: 600; letter-spacing: 0.12em; text-transform: uppercase; color: var(--pop); }
.aud__desc { font-size: 20px; font-weight: 600; letter-spacing: var(--ls-display); margin-top: 8px; line-height: 1.2; }
.aud__list { list-style: none; margin-top: 16px; font-size: 13px; color: var(--sub); }
.aud__list li { padding: 5px 0 5px 18px; position: relative; }
.aud__list li::before { content: '→'; position: absolute; left: 0; color: var(--pop); }

.pricing { display: grid; grid-template-columns: 1fr; gap: 20px; max-width: 820px; margin: 0 auto; }
@media(min-width: 640px){ .pricing { grid-template-columns: 1fr 1fr; } }
.plan { background: var(--card); border: 0.5px solid var(--line); border-radius: 16px; padding: 32px 28px; position: relative; }
.plan--pro { background: var(--grad-dark-2); color: #fff; border: none; overflow: hidden; }
.plan--pro::before { content: ''; position: absolute; top: -40px; right: -40px; width: 160px; height: 160px; border-radius: 50%; background: var(--glow-corner-strong); pointer-events: none; }
.plan--pro > * { position: relative; }
.plan__name { font-size: 12px; font-weight: 600; letter-spacing: 0.12em; text-transform: uppercase; color: var(--muted); }
.plan--pro .plan__name { color: var(--pop-glow); }
.plan__price { display: flex; align-items: baseline; gap: 6px; margin-top: 12px; }
.plan__num { font-family: var(--font-mono); font-size: 40px; font-weight: 600; letter-spacing: var(--ls-num); }
.plan__per { font-size: 13px; color: var(--muted); }
.plan--pro .plan__per { color: var(--dark-fg-muted); }
.plan__note { font-size: 12px; color: var(--muted); margin-top: 4px; }
.plan--pro .plan__note { color: var(--pop-glow); }
.plan__list { list-style: none; margin: 20px 0; font-size: 13.5px; }
.plan__list li { padding: 7px 0 7px 22px; position: relative; color: var(--sub); }
.plan--pro .plan__list li { color: var(--dark-fg); }
.plan__list li::before { content: '✓'; position: absolute; left: 0; color: var(--pop); font-weight: 600; }
.plan__cta { display: block; text-align: center; padding: 11px 20px; border-radius: 9px; font-size: 14px; font-weight: 500; margin-top: 8px; }
.plan__cta--dark { background: var(--grad-btn-dark); color: #fff; }
.plan__cta--pop { background: var(--grad-btn-pop); color: #fff; box-shadow: 0 8px 24px rgba(21,162,74,.35); }

.close-cta { text-align: center; padding: 80px 24px; position: relative; }
.close-cta::before { content: ''; position: absolute; inset: 0; background: radial-gradient(ellipse 700px 400px at 50% 50%, var(--pop-15) 0%, transparent 70%); pointer-events: none; }
.close-cta > * { position: relative; }
.close-cta h2 { font-size: 40px; font-weight: 600; letter-spacing: var(--ls-display-xl); line-height: 1.05; max-width: 600px; margin: 0 auto; }
@media(min-width: 700px){ .close-cta h2 { font-size: 52px; } }
.close-cta p { font-size: 15px; color: var(--sub); margin-top: 16px; }
.close-cta__btn { display: inline-flex; align-items: center; gap: 8px; margin-top: 32px; background: var(--grad-btn-dark); color: #fff; padding: 14px 28px; border-radius: 12px; font-size: 15px; font-weight: 500; box-shadow: var(--shadow-glow); }

.foot { border-top: 0.5px solid var(--line); padding: 32px 0 40px; font-size: 12px; color: var(--muted); display: flex; flex-wrap: wrap; gap: 20px; align-items: center; }
.foot__logo { display: flex; align-items: center; gap: 8px; color: var(--ink); font-weight: 600; }
.foot__logo-mark { width: 20px; height: 20px; border-radius: 5px; background: var(--dark-1); display: flex; align-items: center; justify-content: center; }
.foot__logo-mark span { width: 7px; height: 7px; border-radius: 50%; background: var(--pop); }
.foot__links { display: flex; gap: 18px; margin-left: auto; flex-wrap: wrap; }
</style>
</head>
<body>

<div class="ticker">
  <div class="ticker__wrap" id="ticker">
    <span class="tk-live"><span class="tk-live__dot"></span>Live · USDA</span>
    <span class="tk-item">Loading market data…</span>
  </div>
</div>

<nav class="nav">
  <div class="lp">
    <div class="nav__wrap">
      <a href="/" class="nav__logo">
        <div class="nav__mark"><span></span></div>
        <div class="nav__word">agraX</div>
      </a>
      <div class="nav__links">
        <a href="#checker">Check a price</a>
        <a href="#board">The board</a>
        <a href="#features">Features</a>
        <a href="#pricing">Pricing</a>
      </div>
      <div class="nav__cta">
        <a href="/app" class="sign">Sign in</a>
        <a href="/app" class="btn btn--dark">Open app →</a>
      </div>
    </div>
  </div>
</nav>

<section class="hero">
  <div class="lp">
    <div class="hero__eyebrow"><span class="hero__eyebrow-dot"></span>USDA Market News · updated daily 10am ET</div>
    <br>
    <span class="mood-pill hero__pill" id="hero-pill">● Loading</span>
    <h1>Commodity data for the <span class="hl">produce supply chain</span>.</h1>
    <p class="hero__sub">agraX turns the official USDA Market News feed into a single clean view of today's wholesale market — for buyers, growers, shippers, and analysts.</p>

    <div class="hero__meta">
      <div class="hero__gauge-wrap">
        <div class="hero__gauge">
          <svg width="40" height="40" viewBox="0 0 40 40">
            <circle cx="20" cy="20" r="16" fill="none" stroke="#EFEFEC" stroke-width="4"/>
            <circle id="hero-gauge-arc" cx="20" cy="20" r="16" fill="none" stroke="#15A24A" stroke-width="4" stroke-dasharray="0 100" stroke-linecap="round"/>
          </svg>
          <div class="hero__gauge-txt" id="hero-gauge-num">—</div>
        </div>
        <div class="hero__meta-item">
          <span class="hero__meta-lbl">Mood</span>
          <span class="hero__meta-val" id="hero-mood">—</span>
        </div>
      </div>
      <div class="hero__meta-item">
        <span class="hero__meta-lbl">Items down</span>
        <span class="hero__meta-val pos" id="hero-down">—</span>
      </div>
      <div class="hero__meta-item">
        <span class="hero__meta-lbl">Items tracked</span>
        <span class="hero__meta-val" id="hero-tracked">—</span>
      </div>
      <div class="hero__meta-item">
        <span class="hero__meta-lbl">Markets</span>
        <span class="hero__meta-val" id="hero-markets">15</span>
      </div>
    </div>
  </div>
</section>

<section class="sect" id="checker">
  <div class="lp">
    <div class="sect__head">
      <div class="sect__eyebrow">Free · no signup</div>
      <h2 class="sect__title">Check a price.</h2>
      <p class="sect__sub">Pick a commodity and market. Get today's wholesale range from the official USDA report.</p>
    </div>

    <div class="checker">
      <div class="checker__row">
        <div>
          <div class="checker__label">Commodity</div>
          <select class="checker__sel" id="ck-commodity"><option value="">Loading…</option></select>
        </div>
        <div>
          <div class="checker__label">Market</div>
          <select class="checker__sel" id="ck-market">
            <option value="New York">New York</option>
            <option value="Los Angeles">Los Angeles</option>
            <option value="Chicago">Chicago</option>
            <option value="Miami">Miami</option>
          </select>
        </div>
        <button class="btn btn--dark" id="ck-btn" style="height:41px">Check price</button>
      </div>

      <div class="checker__chips" id="ck-popular"></div>

      <div class="checker__result" id="ck-result">
        <div>
          <div style="font-size:12px;color:var(--muted);margin-bottom:4px" id="ck-out-name">—</div>
          <div style="display:flex;align-items:baseline;gap:10px;flex-wrap:wrap">
            <span class="checker__price" id="ck-out-price">—</span>
            <span class="checker__delta" id="ck-out-delta">—</span>
          </div>
          <div class="checker__meta" id="ck-out-meta">—</div>
        </div>
      </div>
    </div>
    <p style="text-align:center;font-size:12px;color:var(--muted);margin-top:16px">
      Want all 15 markets + FOB origins + history? <a href="/app" style="color:var(--pop);font-weight:500">Open the app →</a>
    </p>
  </div>
</section>

<section class="sect" id="board" style="background:var(--card-alt);border-top:0.5px solid var(--line);border-bottom:0.5px solid var(--line)">
  <div class="lp">
    <div class="sect__head">
      <div class="sect__eyebrow">The board · today</div>
      <h2 class="sect__title">Where prices are moving.</h2>
      <p class="sect__sub">Top movers across four major terminal markets. Updated every morning.</p>
    </div>
    <div class="board" id="board-grid"></div>
    <p style="text-align:center;font-size:13px;color:var(--muted);margin-top:24px">
      Pro unlocks all 15 markets + shipping-point (FOB) origins. <a href="#pricing" style="color:var(--pop);font-weight:500">See pricing →</a>
    </p>
  </div>
</section>

<section class="sect">
  <div class="lp">
    <div class="sect__head">
      <div class="sect__eyebrow">Coverage</div>
      <h2 class="sect__title">15 terminal markets. 5 shipping origins.</h2>
      <p class="sect__sub">Every USDA Market News fruit &amp; vegetable report, ingested daily.</p>
    </div>

    <div class="map-card">
      <svg class="map-svg" viewBox="0 0 720 400" xmlns="http://www.w3.org/2000/svg">
        <path d="M 60 180 Q 80 140 130 130 L 200 120 Q 260 110 320 115 L 420 118 Q 490 120 560 135 L 620 155 Q 660 180 665 220 L 655 260 Q 620 300 570 320 L 480 335 Q 400 345 320 340 L 220 335 Q 140 320 90 285 Q 55 250 60 180 Z"
              fill="none" stroke="rgba(21,162,74,0.25)" stroke-width="1" stroke-dasharray="3 4"/>
        <g>
          <circle cx="590" cy="175" r="6" class="map-pulse"/><text x="600" y="178" font-family="Inter" font-size="10" fill="#0A0B0A" font-weight="600">New York</text>
          <circle cx="570" cy="190" r="5"/><text x="580" y="205" font-family="Inter" font-size="9" fill="#4a5045">Philadelphia</text>
          <circle cx="555" cy="200" r="5"/><text x="500" y="215" font-family="Inter" font-size="9" fill="#4a5045">Baltimore</text>
          <circle cx="605" cy="160" r="5"/><text x="615" y="163" font-family="Inter" font-size="9" fill="#4a5045">Boston</text>
          <circle cx="475" cy="170" r="5"/><text x="485" y="173" font-family="Inter" font-size="9" fill="#4a5045">Detroit</text>
          <circle cx="430" cy="180" r="6" class="map-pulse"/><text x="380" y="175" font-family="Inter" font-size="10" fill="#0A0B0A" font-weight="600">Chicago</text>
          <circle cx="500" cy="270" r="5"/><text x="510" y="273" font-family="Inter" font-size="9" fill="#4a5045">Atlanta</text>
          <circle cx="560" cy="335" r="6" class="map-pulse"/><text x="570" y="338" font-family="Inter" font-size="10" fill="#0A0B0A" font-weight="600">Miami</text>
          <circle cx="535" cy="235" r="5"/><text x="545" y="238" font-family="Inter" font-size="9" fill="#4a5045">Raleigh</text>
          <circle cx="525" cy="255" r="5"/><text x="470" y="258" font-family="Inter" font-size="9" fill="#4a5045">Columbia</text>
          <circle cx="500" cy="240" r="5"/><text x="440" y="243" font-family="Inter" font-size="9" fill="#4a5045">Asheville</text>
          <circle cx="130" cy="255" r="6" class="map-pulse"/><text x="60" y="258" font-family="Inter" font-size="10" fill="#0A0B0A" font-weight="600">Los Angeles</text>
          <circle cx="100" cy="215" r="5"/><text x="30" y="218" font-family="Inter" font-size="9" fill="#4a5045">San Francisco</text>
          <circle cx="115" cy="230" r="4" fill="#B87A0B"/><text x="125" y="233" font-family="Inter" font-size="8" fill="#4a5045" font-style="italic">Fresno (FOB)</text>
          <circle cx="200" cy="275" r="4" fill="#B87A0B"/><text x="210" y="278" font-family="Inter" font-size="8" fill="#4a5045" font-style="italic">Phoenix (FOB)</text>
          <circle cx="215" cy="305" r="4" fill="#B87A0B"/><text x="225" y="308" font-family="Inter" font-size="8" fill="#4a5045" font-style="italic">Nogales (FOB)</text>
          <circle cx="540" cy="305" r="4" fill="#B87A0B"/><text x="550" y="308" font-family="Inter" font-size="8" fill="#4a5045" font-style="italic">Orlando (FOB)</text>
          <circle cx="515" cy="285" r="4" fill="#B87A0B"/><text x="435" y="288" font-family="Inter" font-size="8" fill="#4a5045" font-style="italic">Thomasville (FOB)</text>
        </g>
      </svg>
      <div class="map-caption">
        <span style="color:var(--pop)">●</span> Terminal market &nbsp;&nbsp;
        <span style="color:#B87A0B">●</span> Shipping-point origin (FOB)
      </div>
    </div>
  </div>
</section>

<section class="sect" style="background:var(--card-alt);border-top:0.5px solid var(--line);border-bottom:0.5px solid var(--line)">
  <div class="lp">
    <div class="sect__head">
      <div class="sect__eyebrow">Who it's for</div>
      <h2 class="sect__title">Built for the whole chain.</h2>
    </div>
    <div class="audiences">
      <div class="aud">
        <div class="aud__who">Buyers</div>
        <div class="aud__desc">Know today's wholesale range before you call the packer.</div>
        <ul class="aud__list"><li>Cross-market comparison</li><li>Buy-signal alerts</li><li>Cost tracking</li></ul>
      </div>
      <div class="aud">
        <div class="aud__who">Growers</div>
        <div class="aud__desc">See what the market is paying at origin, in real time.</div>
        <ul class="aud__list"><li>FOB shipping-point pricing</li><li>Competing-origin view</li><li>Season-over-season trends</li></ul>
      </div>
      <div class="aud">
        <div class="aud__who">Shippers</div>
        <div class="aud__desc">Spot arbitrage windows between origin and terminal.</div>
        <ul class="aud__list"><li>Origin-to-terminal spreads</li><li>Movement volume tracking</li><li>National trends commentary</li></ul>
      </div>
      <div class="aud">
        <div class="aud__who">Analysts</div>
        <div class="aud__desc">Clean historical data, without wrestling the raw USDA feed.</div>
        <ul class="aud__list"><li>Full history export (CSV)</li><li>API access</li><li>Custom watchlists</li></ul>
      </div>
    </div>
  </div>
</section>

<section class="sect" id="features">
  <div class="lp">
    <div class="sect__head">
      <div class="sect__eyebrow">Product</div>
      <h2 class="sect__title">What you get.</h2>
    </div>
    <div class="features">
      <div class="feature">
        <div class="feature__ico">◈</div>
        <div class="feature__title">Cross-market comparison</div>
        <p class="feature__desc">See prices for one commodity across every U.S. terminal at once. Sorted cheapest-first, with the best-price row flagged.</p>
        <div class="feature__mini">
          <div class="feature__mini-row"><span>New York</span><span><b>$28.50</b> <span class="pos">Best</span></span></div>
          <div class="feature__mini-row"><span>Chicago</span><span><b>$30.00</b></span></div>
          <div class="feature__mini-row"><span>Los Angeles</span><span><b>$32.75</b></span></div>
          <div class="feature__mini-row"><span>Miami</span><span><b>$34.20</b></span></div>
        </div>
      </div>
      <div class="feature">
        <div class="feature__ico">★</div>
        <div class="feature__title">Watchlists &amp; alerts</div>
        <p class="feature__desc">Pin the commodities you care about. Get pinged when prices cross the threshold you set.</p>
        <div class="feature__mini">
          <div class="feature__mini-row"><span>★ Hass avocados</span><span class="pos">▼ 12.4%</span></div>
          <div class="feature__mini-row"><span>★ Strawberries</span><span class="neg">▲ 8.2%</span></div>
          <div class="feature__mini-row"><span>★ Iceberg lettuce</span><span class="pos">▼ 3.1%</span></div>
          <div class="feature__mini-row"><span>★ Roma tomatoes</span><span style="color:var(--muted)">— 0.4%</span></div>
        </div>
      </div>
      <div class="feature">
        <div class="feature__ico">◆</div>
        <div class="feature__title">Movement &amp; volume</div>
        <p class="feature__desc">Physical flow data alongside price — the leading indicator that tells you what tomorrow's numbers will do.</p>
        <div class="feature__mini">
          <div class="feature__mini-row"><span>Truck arrivals</span><span><b>1,247</b> <span class="pos">▲ 8%</span></span></div>
          <div class="feature__mini-row"><span>Rail arrivals</span><span><b>184</b> <span class="neg">▼ 3%</span></span></div>
          <div class="feature__mini-row"><span>Import volume</span><span><b>612 lbs</b> <span class="pos">▲ 22%</span></span></div>
        </div>
      </div>
      <div class="feature">
        <div class="feature__ico">↗</div>
        <div class="feature__title">Export &amp; API</div>
        <p class="feature__desc">Pull the raw data. CSV downloads, a clean REST API, and full history — no more scraping USDA text files.</p>
        <div class="feature__mini">
          <div class="feature__mini-row"><span>GET /reports/terminal</span><span class="pos">200 OK</span></div>
          <div class="feature__mini-row"><span>GET /history/avocados</span><span class="pos">200 OK</span></div>
          <div class="feature__mini-row"><span>GET /markets</span><span class="pos">200 OK</span></div>
        </div>
      </div>
    </div>
  </div>
</section>

<section class="sect" id="pricing" style="background:var(--card-alt);border-top:0.5px solid var(--line);border-bottom:0.5px solid var(--line)">
  <div class="lp">
    <div class="sect__head">
      <div class="sect__eyebrow">Pricing</div>
      <h2 class="sect__title">Simple.</h2>
      <p class="sect__sub">Free forever for the essentials. Pro when you're ready to trade on it.</p>
    </div>

    <div class="pricing">
      <div class="plan">
        <div class="plan__name">Free</div>
        <div class="plan__price"><span class="plan__num">$0</span><span class="plan__per">forever</span></div>
        <div class="plan__note">No signup required</div>
        <ul class="plan__list">
          <li>4 major terminals (NY, LA, Chicago, Miami)</li>
          <li>Today's prices + 7-day sparkline</li>
          <li>Movement summary</li>
          <li>Cross-market price checker</li>
        </ul>
        <a href="/app" class="plan__cta plan__cta--dark">Open the app →</a>
      </div>
      <div class="plan plan--pro">
        <div class="plan__name">★ Pro</div>
        <div class="plan__price"><span class="plan__num">$29</span><span class="plan__per">/month</span></div>
        <div class="plan__note">14-day trial · no card required</div>
        <ul class="plan__list">
          <li>All 15 terminals + 5 FOB origins</li>
          <li>Full price history + custom windows</li>
          <li>Watchlists &amp; price alerts</li>
          <li>CSV export + API access</li>
          <li>Movement &amp; volume data</li>
        </ul>
        <a href="/app" class="plan__cta plan__cta--pop">Start free trial →</a>
      </div>
    </div>
  </div>
</section>

<section class="close-cta">
  <div class="lp">
    <h2>Stop scraping USDA text files.</h2>
    <p>Open the app. See today's market in 30 seconds.</p>
    <a href="/app" class="close-cta__btn">Open agraX →</a>
  </div>
</section>

<footer>
  <div class="lp">
    <div class="foot">
      <div class="foot__logo"><div class="foot__logo-mark"><span></span></div>agraX</div>
      <span>© 2026 · Data: USDA Market News (public domain)</span>
      <div class="foot__links">
        <a href="/app">App</a>
        <a href="#pricing">Pricing</a>
        <a href="#features">Features</a>
      </div>
    </div>
  </div>
</footer>

<script src="/js/config.js"></script>
<script src="/js/api.js"></script>
<script src="/js/util.js"></script>
<script>
(async function() {
  const U = window.agraxUtil;
  const FREE_CITIES = ['New York', 'Los Angeles', 'Chicago', 'Miami'];

  let today = [], prev = [];
  try {
    const dates = await agraxAPI.dates();
    if (!dates?.length) throw new Error('no data');
    today = await agraxAPI.reportTerminal(dates[0].date);
    if (dates[1]) prev = await agraxAPI.reportTerminal(dates[1].date);
  } catch (err) {
    document.getElementById('ticker').innerHTML =
      `<span class="tk-live"><span class="tk-live__dot"></span>Live · USDA</span><span class="tk-item">Data feed connecting…</span>`;
    console.error('[agraX] API fetch failed:', err, 'base:', agraxAPI.base);
    // Also surface a small visible hint in the hero so you know when it's broken
    const heroMood = document.getElementById('hero-mood');
    if (heroMood) heroMood.textContent = 'API unreachable';
    return;
  }

  // Hero stats (default: New York)
  const heroMkt = 'New York';
  const heroToday = today.filter(r => r.market === heroMkt);
  const heroPrev = prev.filter(r => r.market === heroMkt);
  const heroTodayBy = U.groupBy(heroToday, r => r.commodity);
  const heroPrevBy = U.groupBy(heroPrev, r => r.commodity);

  const changes = [];
  for (const [name, rs] of heroTodayBy) {
    const agg = U.aggregateCommodity(rs);
    const pAgg = heroPrevBy.has(name) ? U.aggregateCommodity(heroPrevBy.get(name)) : null;
    const d = U.delta(agg.mostly, pAgg?.mostly);
    changes.push({ name, today: agg, pct: d.pct, dir: d.dir });
  }
  const withChg = changes.filter(c => c.pct != null);
  const downN = withChg.filter(c => c.dir === 'down').length;
  const avgChg = withChg.length ? withChg.reduce((s, c) => s + c.pct, 0) / withChg.length : null;
  const mood = U.moodFromAvgChg(avgChg);

  document.getElementById('hero-pill').textContent = `● ${mood.label}`;
  document.getElementById('hero-mood').textContent = mood.label;
  document.getElementById('hero-down').textContent = downN;
  document.getElementById('hero-tracked').textContent = changes.length;
  document.getElementById('hero-markets').textContent = new Set(today.map(r => r.market)).size;
  document.getElementById('hero-gauge-num').textContent = mood.score;
  const circ = 2 * Math.PI * 16;
  const fill = (mood.score / 100) * circ;
  document.getElementById('hero-gauge-arc').setAttribute('stroke-dasharray', `${fill.toFixed(1)} ${(circ - fill).toFixed(1)}`);

  // Ticker + board data
  const allChanges = [];
  for (const city of FREE_CITIES) {
    const tRows = today.filter(r => r.market === city);
    const pRows = prev.filter(r => r.market === city);
    const tBy = U.groupBy(tRows, r => r.commodity);
    const pBy = U.groupBy(pRows, r => r.commodity);
    for (const [name, rs] of tBy) {
      const agg = U.aggregateCommodity(rs);
      const pAgg = pBy.has(name) ? U.aggregateCommodity(pBy.get(name)) : null;
      const d = U.delta(agg.mostly, pAgg?.mostly);
      if (d.pct != null && agg.mostly != null) allChanges.push({ name, market: city, pct: d.pct, mostly: agg.mostly });
    }
  }

  // Ticker: biggest movers
  const sorted = [...allChanges].sort((a, b) => Math.abs(b.pct) - Math.abs(a.pct));
  const tickerItems = sorted.slice(0, 12);
  if (tickerItems.length) {
    const html = tickerItems.map(t => {
      const cls = t.pct < 0 ? 'up' : 'dn';
      const arrow = t.pct < 0 ? '▼' : '▲';
      return `<span class="tk-item"><b>${escape(t.name)}</b> · ${escape(t.market)} · $${t.mostly.toFixed(2)} <span class="${cls}">${arrow} ${Math.abs(t.pct).toFixed(1)}%</span></span>`;
    }).join('');
    document.getElementById('ticker').innerHTML =
      `<span class="tk-live"><span class="tk-live__dot"></span>Live · USDA</span>${html}<span class="tk-live"><span class="tk-live__dot"></span>Live · USDA</span>${html}`;
  }

  // Price checker
  const allCommodities = Array.from(new Set(today.map(r => r.commodity))).sort();
  const ckC = document.getElementById('ck-commodity');
  ckC.innerHTML = '<option value="">Select a commodity…</option>' +
    allCommodities.map(c => `<option value="${escape(c)}">${escape(c)}</option>`).join('');

  const popularSeeds = ['Strawberries', 'Avocados', 'Tomatoes', 'Lettuce', 'Potatoes', 'Onions'];
  const pop = document.getElementById('ck-popular');
  const popMatches = [];
  for (const seed of popularSeeds) {
    const match = allCommodities.find(c => c.toLowerCase().includes(seed.toLowerCase()));
    if (match && !popMatches.includes(match)) popMatches.push(match);
  }
  pop.innerHTML = popMatches.slice(0, 6).map(n => `<span class="checker__chip" data-c="${escape(n)}">${escape(n)}</span>`).join('');

  function runChecker() {
    const commodity = ckC.value;
    const market = document.getElementById('ck-market').value;
    if (!commodity) return;
    const matches = today.filter(r => r.commodity === commodity && r.market === market);
    const prevMatches = prev.filter(r => r.commodity === commodity && r.market === market);
    const res = document.getElementById('ck-result');
    res.classList.add('show');
    if (!matches.length) {
      document.getElementById('ck-out-name').textContent = `${commodity} · ${market}`;
      document.getElementById('ck-out-price').textContent = 'No data';
      document.getElementById('ck-out-delta').textContent = '';
      document.getElementById('ck-out-meta').textContent = `Not reported in ${market} today. Try a different market.`;
      return;
    }
    const agg = U.aggregateCommodity(matches);
    const pAgg = prevMatches.length ? U.aggregateCommodity(prevMatches) : null;
    const d = U.delta(agg.mostly, pAgg?.mostly);
    document.getElementById('ck-out-name').textContent = `${commodity} · ${market}`;
    document.getElementById('ck-out-price').textContent = U.fmtPrice(agg.mostly);
    const delta = document.getElementById('ck-out-delta');
    if (d.pct != null) {
      const arrow = d.dir === 'up' ? '▲' : d.dir === 'down' ? '▼' : '—';
      delta.textContent = `${arrow} ${Math.abs(d.pct).toFixed(1)}% vs yesterday`;
      delta.className = 'checker__delta ' + (d.dir === 'down' ? 'dn' : d.dir === 'up' ? 'up' : 'fl');
    } else {
      delta.textContent = 'No prior data';
      delta.className = 'checker__delta fl';
    }
    const packs = U.uniq(matches.map(m => m.package).filter(Boolean));
    document.getElementById('ck-out-meta').textContent =
      `Range: ${U.fmtPrice(agg.low)}–${U.fmtPrice(agg.high)} · ${packs[0] || ''} · ${matches.length} lot${matches.length === 1 ? '' : 's'}`;
  }
  document.getElementById('ck-btn').addEventListener('click', runChecker);
  ckC.addEventListener('change', runChecker);
  document.getElementById('ck-market').addEventListener('change', runChecker);
  pop.addEventListener('click', (e) => {
    const chip = e.target.closest('[data-c]');
    if (!chip) return;
    ckC.value = chip.dataset.c;
    runChecker();
  });

  // The board
  const board = document.getElementById('board-grid');
  board.innerHTML = FREE_CITIES.map(city => {
    const cityChanges = allChanges.filter(c => c.market === city);
    cityChanges.sort((a, b) => Math.abs(b.pct) - Math.abs(a.pct));
    const top = cityChanges.slice(0, 5);
    const rowsHtml = top.length ? top.map(t => {
      const cls = t.pct < 0 ? 'chg-down' : t.pct > 0 ? 'chg-up' : 'chg-flat';
      const arrow = t.pct < 0 ? '▼' : t.pct > 0 ? '▲' : '—';
      return `<div class="board__row">
        <span class="board__row-name">${escape(t.name)}</span>
        <span class="board__row-price">$${t.mostly.toFixed(2)}</span>
        <span class="board__row-chg ${cls}">${arrow} ${Math.abs(t.pct).toFixed(1)}%</span>
      </div>`;
    }).join('') : `<div style="padding:20px;text-align:center;color:var(--muted);font-size:12px">No comparison available</div>`;
    return `<div class="board__col">
      <div class="board__hd">
        <span class="board__pin"></span>
        <span class="board__city">${escape(city)}</span>
        <span class="board__count">${cityChanges.length} tracked</span>
      </div>
      ${rowsHtml}
    </div>`;
  }).join('');

  function escape(s) { return String(s ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])); }
})();
</script>
</body>
</html>
